#!/bin/sh

status=$(mpc status | grep playing)

if [ -n "$status" ]
then
	printf "󰫔"
else
	printf "󰐎"
fi

case $BUTTON in
	1)
		xdotool key super+m
		break;;
	3)
		notify-send "Now Playing 🎶" "$(mpc current)"
		break;;
esac
